# Версии из приложения A.2 курсовой работы.
# Эксперименты запускались на Kaggle T4 / Google Colab с Python 3.11/3.12.
#
# ВАЖНО: neuralforecast ставить отдельно с --no-deps, иначе тянет несовместимые
# версии numpy/pandas:
#   pip install --no-deps neuralforecast==1.7.5

# --- core ---
numpy>=1.26,<2.2
pandas>=2.0,<2.4
scipy>=1.11,<1.14
scikit-learn>=1.3,<1.6
matplotlib>=3.7

# --- deep learning ---
torch>=2.1
pytorch-lightning>=2.0,<3.0

# --- forecasting ---
# neuralforecast==1.7.5  # см. примечание выше, ставить отдельно с --no-deps
chronos-forecasting
ray
utilsforecast
coreforecast

# --- time-series classification ---
aeon
tslearn>=0.6,<0.7
pyts>=0.13,<0.14

# --- финансовые данные ---
python-binance
yfinance

# --- утилиты ---
tqdm
